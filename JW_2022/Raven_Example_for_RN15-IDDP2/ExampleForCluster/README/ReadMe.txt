#This is a example for submitting a Raven job to BwUniCluster 2.0.

The following shows the way to sumbit a job on a loging node, which is working at least until 2022/2/20:

Firstly, run "conda activate raven_libraries" 
Then, run "sbatch -p xxx(--partition) ConstructROM.sh"


